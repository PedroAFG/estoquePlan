import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#1976d2" }, // muda depois
    secondary: { main: "#2e7d32" },
    background: { default: "#f6f7fb" },
  },
  shape: { borderRadius: 12 },
});